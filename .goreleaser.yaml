builds:
- main: ./cmd/fedach/
  ldflags:
    - -s -w # strip debug info + DWARF
    - -X main.Version={{ .Version }}
    - -X main.Commit={{ .FullCommit }}
    - -X main.Date={{ .Date }}
    - -X github.com/moov-io/fedach.Version={{ .Tag }}
